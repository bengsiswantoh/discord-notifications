require("dotenv").config();
const fs = require("fs");

const main = (filename, data) => {
  data[name] = newData;
  fs.writeFileSync(process.env.FILENAME, JSON.stringify(data));
};

module.exports = main;
